What to include in test data:

Types of data:
    - Usernames/Passwords in plain text
    - API keys/tokens (e.g., AWS keys, OAuth tokens) - check format så det kan emulates
    - Cryptographic keys (Private keys, encryption keys, or SSH keys)
    - Session/Token Secrets - check format så det kan emulates
    - Obfuscated/Encoded Secrets (e.g. base 64 encided stuff) (MAYBE)

Types of files:
    - source code - her er det c# filer
    - configuration files
    - Version control in github?
    - Compiled Binaries?
